# 兼容性矩阵

[简体中文](COMPATIBILITY.md) · [繁體中文](../Zh-tw/COMPATIBILITY.md) · [English](../en/COMPATIBILITY.md) · [返回简中 README](README.md)

本文只记录实际完成的验证，不把“能够编译”写成“已经兼容”。

## 1.3.6 离线启动与运行环境修复

- 离线引擎首次启动失败后，兼容模式不再重复传递新版可选参数，可兼容启动器缓存中的旧版 llama.cpp；
- 启动失败、超时、权限、缺失文件、CPU 指令和内存终止均提供中文诊断，并只读取本次启动追加的日志；
- 1.21.x Forge／NeoForge 发布目标使用 Java 21 验证。请不要用 Java 25 替代，旧版 LWJGL 可能在产生 Java 堆栈前由原生层终止；
- 更新时必须从 `mods` 目录移除 1.3.6 及更早版本，只保留一个与加载器及游戏版本匹配的 1.3.7 JAR。

## 1.3.5 设置选择修复

- 设置页自绘文字统一使用显式不透明 ARGB 颜色；会读取 Alpha 通道的新版 Minecraft 现在可以正常显示翻译服务名称和当前选中项；
- 新增源码验证任务，覆盖正式发布的 Fabric、Forge 与 NeoForge 设置选择实现，禁止六位透明文字颜色再次进入构建。

## 1.3.4 新增的离线与界面兼容修复

- Windows 游戏目录包含中文或其他非 ASCII 字符时，已校验的 GGUF 模型会通过同盘硬链接
  （不支持时使用一次性兼容副本）提供给本地原生引擎，避免 llama.cpp 收到乱码模型路径；
- Android ARM64 Java 启动器不再被误判为 Ubuntu ARM64，而是使用同一固定 llama.cpp 版本的
  官方 Android ARM64 构建及 SHA-256；原生引擎安装到应用私有缓存，避免共享存储的 `noexec` 限制；
- 上述路径、平台选择和动态库路径已进入核心自测并由所有版本共享。Android 实机仍会受到
  启动器权限、Android 版本和设备可用内存影响，因此手机端完整游戏内回归仍需继续收集结果。

## 1.3.4 发布验证

| Minecraft | 加载器 | Java | 构建与自测 | 实际启动到主菜单 | 服务器内完整人工回归 |
| --- | --- | --- | --- | --- | --- |
| 1.8.9 | Forge 11.15.1.2318 | 8 | 通过 | 历史基线通过 | 待确认 |
| 1.12.2 | Forge 14.23.5.2860 | 8 | 通过 | 历史基线通过 | 待确认 |
| All 1.14/1.15 releases (1.14-1.15.2), single JAR | Fabric Loader 0.19.3 + Fabric API 0.28.5 | 8 | Passed; exact Loader selection passed for all eight versions | Pending | Pending |
| All 1.16 releases (1.16-1.16.5), single JAR | Fabric Loader 0.19.3 + Fabric API 0.42.0 | 8 | Passed; exact Loader selection passed for all six versions | Pending | Pending |
| 1.16.5 | Fabric Loader 0.19.3 + Fabric API 0.42.0 | 8 | 通过，metadata 与 Mixin 已检查 | 待验证 | 待验证 |
| 单一 1.17/1.18 JAR（1.17–1.18.2） | Fabric Loader 0.19.3 | 17 | 已通过；五个版本均通过精确 Loader 选择 | 待验证 | 待验证 |
| 1.16.5 | Forge 36.2.42 | 8 | 通过，SRG 与 refmap 已检查 | 待验证 | 待验证 |
| 1.19.2 | Fabric Loader 0.19.3 + Fabric API 0.77.0 | 17 | 通过，metadata 与 Mixin 已检查 | 待验证 | 待验证 |
| 单一 1.19 JAR（1.19–1.19.4） | Fabric Loader 0.19.3 | 17 | 已通过；五个版本均通过精确 Loader 选择 | 待验证 | 待验证 |
| 1.19.2 | Forge 43.5.2 | 17 | 通过，SRG 与 refmap 已检查 | 待验证 | 待验证 |
| 1.20.1 | Fabric Loader 0.18.1 + Fabric API 0.92.11 | 17 | 通过 | 待验证 | 待验证 |
| 单一 1.20 JAR（1.20–1.20.6） | Fabric Loader 0.15.11+ | 17 / 21 | 已通过；七个版本均通过精确 Loader 选择 | 待验证 | 待验证 |
| 1.20.1 | Forge 47.4.10 | 17 | 通过，SRG 与 refmap 已检查 | 待验证 | 待验证 |
| 1.20.1 | NeoForge 47.1.106 | 17 | 通过，metadata、Mixin 与 refmap 已检查 | 通过，模组初始化完成 | 待验证 |
| 1.21.1 | NeoForge 21.1.248 | 21 | 通过，metadata 与 Mixin 已检查 | 待验证 | 待验证 |
| 1.21.11 | Fabric Loader 0.18.1 + Fabric API 0.141.4 | 21 | 通过 | 历史基线通过 | 待确认 |
| 全部 1.21 版本（1.21–1.21.11）单一 JAR | Fabric Loader 0.19.3 | 21 | 通过；十二个版本的 Loader 选择测试均通过 | 待验证 | 待验证 |
| 1.21、1.21.1、1.21.3–1.21.11 | Forge 51.0.33–61.2.0 | 21 | 所有已发布游戏目标均编译通过；元数据范围精确 | 1.21.11 客户端已初始化并完成资源、音频与图集重载 | 待验证 |
| 26.1、26.1.1、26.1.2、26.2 | Forge 62.0.9–65.1.1 | 25 | 四个精确目标全部编译通过 | 待验证 | 待验证 |
| 26.1、26.1.1、26.1.2、26.2 单一 JAR | Fabric Loader 0.19.3 | 25 | 通过；四个版本的 Loader 选择测试均通过 | 26.1 历史基线通过 | 待验证 |

正式版以 30 个共享同一翻译核心的发布目标 JAR 为输入，并整理为 15 个可直接安装的发布 JAR；
额外的邻近版本源码目标单独完成构建测试，不计入正式下载。1.20.1 Fabric 已完成 Loom 重映射，1.20.1 Forge
产物已重命名为该版本运行时使用的 SRG 名称且包含九个 Mixin 类的 refmap；1.21.11
Forge 已适配 Forge 7 事件总线和 Mojmap 运行时。1.21.11 Forge 开发客户端已初始化
1.3.4 模组并完成资源、音频和图集重载，随后由自动化测试主动停止；完整的服务器内手工回归仍待完成。

1.3.4 的在线服务统一工厂、自定义 JSON 模板、嵌套响应路径、签名摘要、端点／请求头安全、
有限重试与配置迁移均通过不使用真实密钥和外网的核心自测。服务商端到端调用仍需用户自己的
账号、额度和区域，因此本表不把“本地协议测试通过”写成“真实付费 API 已调用成功”。

1.21.x 单一 JAR 内嵌十二个精确版本实现，而不是把同一套 Mixin 放宽到不兼容的游戏 API。
每个内嵌实现只声明一个 Minecraft 版本，再由 Fabric Loader 为 1.21、1.21.1、1.21.2、
1.21.3、1.21.4、1.21.5、1.21.6、1.21.7、1.21.8、1.21.9、1.21.10 或 1.21.11 选出精确匹配项。

各加载器的元数据都固定到完成构建验证的精确 Minecraft 版本；待完成的启动和服务器内
人工回归会继续如实标注，不会据此猜测相邻版本兼容性。

## “支持所有版本”的原则

Minecraft 各时代的加载器、Java 版本、渲染 API 和文字组件结构不同，无法用一个
JAR 安全覆盖所有 Java 版。项目会按有代表性的长期使用版本逐条适配，并为每条发布线
保留独立 JAR。只有完成构建、启动、服务器内界面回归后，才会进入“已验证”表。

后续候选顺序：

1. 1.12.2 Cleanroom；
2. 1.7.10 Forge；
3. 1.16.5 Forge；
4. 1.18.2 Forge/Fabric；
5. 1.21.1 Fabric/NeoForge；
6. 后续仍有较多玩家使用的新版本。

“相邻版本”不会通过放宽 `fabric.mod.json` 或 `mods.toml` 的版本范围来猜测支持。
例如 1.20.1 的 JAR 明确拒绝 1.20.2，Fabric 与 Forge 也绝不混用。这样能避免方法描述符
或事件 ABI 的细微变化在启动时导致 Mixin 失败。相邻版本会复用共享核心，但必须新增
独立平台适配并通过同一套构建、映射、启动和服务器内回归后才能正式列入。

## 服务器内回归清单

- 聊天、记分板、Tab 列表、Action Bar、标题和 Boss Bar；
- 箱子/菜单标题、物品名与 Lore、告示牌、书、实体名和全息文字；
- 未进入世界时的模组设置页、任务书、配方界面和整合包自定义标题屏；
- 模组/整合包文字使用原版字体渲染器时可捕获，图片文字和自带渲染器明确列为限制；
- 玩家自己输入和发出的聊天保持原文；
- 在线玩家名称和本机名称默认保持原样，玩家名翻译开关生效；服务器 IP/域名/端口始终保持原样；
- 已有中文不重复翻译，英文片段可翻译；
- 仅译文模式无双语溢出，颜色选项生效；
- `U` 控制面板和 `F8` 总开关在不同窗口尺寸下可用；
- 模型下载中、下载失败、离线引擎失败时游戏不冻结，继续显示原文。
